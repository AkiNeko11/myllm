# 快速开始指南

## 第一步：准备您的角色语料

1. 收集角色的对话数据（微博、小说对话、聊天记录等）
2. 整理成JSON格式，保存为 `data/raw/conversations.json`

**示例格式**：

```json
[
  {
    "conversation": [
      {"role": "user", "content": "用户说的话"},
      {"role": "assistant", "content": "角色的回复"}
    ]
  },
  {
    "conversation": [
      {"role": "user", "content": "另一段对话"},
      {"role": "assistant", "content": "角色的回复"},
      {"role": "user", "content": "继续对话"},
      {"role": "assistant", "content": "角色的回复"}
    ]
  }
]
```

**数据要求**：
- ✅ 至少100轮对话（越多越好，但质量比数量重要）
- ✅ 体现角色的语言特点、性格、说话方式
- ✅ 对话要自然、连贯
- ❌ 避免无关内容、广告、乱码

## 第二步：配置角色信息

编辑 `config/train_config.yaml`，修改以下部分：

```yaml
character:
  name: "鲁迅"  # 改成你的角色名
  description: "中国现代文学巨匠"
  system_prompt: "你是鲁迅，你的语言犀利深刻，善于批判现实，文字简洁有力。"

data:
  raw_data_path: "data/raw/conversations.json"
```

## 第三步：一键训练（三条命令）

```bash
# 1. 安装依赖（首次运行）
pip install -r requirements.txt

# 2. 处理数据
python scripts/prepare_data.py

# 3. 开始训练
python scripts/train.py
```

训练时间参考：
- 100条对话：约10-20分钟（4060笔记本）
- 500条对话：约30-60分钟
- 1000条对话：约1-2小时

## 第四步：测试效果

```bash
python scripts/inference.py \
  --model_path checkpoints/示例角色_Qwen1.5-1.8B-Chat/final_model \
  --load_in_4bit
```

然后就可以和你的角色对话了！

## 常见问题速查

### ❌ 显存不够

编辑 `config/train_config.yaml`：

```yaml
training:
  per_device_train_batch_size: 2  # 从4改成2
  gradient_accumulation_steps: 8  # 从4改成8
```

### ❌ 模型下载慢

```bash
# Windows PowerShell
$env:HF_ENDPOINT="https://hf-mirror.com"

# 或者手动下载模型到本地，然后修改配置：
```

```yaml
model:
  base_model: "本地路径/Qwen-1.8B-Chat"
```

### ❌ 效果不好

1. **增加数据量**：至少500轮高质量对话
2. **调整system_prompt**：更详细地描述角色特点
3. **增加训练轮数**：从3改成5
4. **检查数据质量**：是否真正体现角色风格

## 租卡训练指南

如果笔记本性能不够，推荐使用云GPU：

### AutoDL（推荐新手）

1. 注册AutoDL：https://www.autodl.com/
2. 创建实例，选择 RTX 3090/4090（约1-2元/小时）
3. 上传代码：

```bash
# 在AutoDL终端
git clone 你的项目地址
cd myllm
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

4. 上传数据到 `data/raw/`
5. 运行训练：`python scripts/train.py`
6. 训练完成后下载 `checkpoints/` 目录

### 成本估算

- 100条对话：约0.3-0.5元（10-20分钟）
- 500条对话：约1-2元（30-60分钟）
- 1000条对话：约2-4元（1-2小时）

## 完整流程图

```
准备语料 → 修改配置 → 处理数据 → 训练模型 → 测试效果
   ↓           ↓           ↓           ↓           ↓
 JSON文件   train_config  prepare_data  train.py  inference.py
```

## 下一步

训练完成后，您可以：

1. **继续优化**：调整参数，增加数据，重新训练
2. **合并模型**：使用 `merge_lora.py` 生成完整模型
3. **部署使用**：集成到你的应用中
4. **分享交流**：和社区分享你的角色模型

祝训练顺利！🎉

